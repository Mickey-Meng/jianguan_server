<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.jianguan.quality.mapper.QualityActivityMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.ruoyi.jianguan.quality.domain.entity.QualityActivity">
        <id column="id" property="id" />
        <result column="create_user_id" property="createUserId" />
        <result column="create_time" property="createTime" />
        <result column="update_user_id" property="updateUserId" />
        <result column="update_time" property="updateTime" />
        <result column="project_id" property="projectId" />
        <result column="build_section" property="buildSection" />
        <result column="activity_info" property="activityInfo" />
        <result column="remark" property="remark" />
        <result column="attachment" property="attachment" />
        <result column="status" property="status" />
        <result column="draft_flag" property="draftFlag" />
        <result column="deleted_flag" property="deletedFlag" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        create_user_id,
        create_time,
        update_user_id,
        update_time,
        id, project_id, build_section, activity_info, remark, attachment, status, draft_flag, deleted_flag
    </sql>

    <select id="getPageInfo" parameterType="com.ruoyi.jianguan.quality.domain.dto.QualityActivityPageDTO" resultType="com.ruoyi.jianguan.quality.domain.vo.QualityActivityPageVo">
        select a.*,p.NAME as buildSectionName,u.name as createName,u.NAME as createUserName
        from zz_quality_activity a
        left join ss_f_projects p on a.build_section = p.id and p.GROUPLEVEL = 3
        left join ss_f_users u on u.id = a.create_user_id
        where a.project_id = #{pageDto.projectId} and a.build_section = #{pageDto.buildSection}
        <if test="pageDto.buildSectionName != null and pageDto.buildSectionName != ''">
            and p.name like concat('%',#{pageDto.buildSectionName},'%')
        </if>
        <if test="pageDto.createUserName != null and pageDto.createUserName != ''">
            and u.name like concat('%',#{pageDto.createUserName},'%')
        </if>
        <if test="pageDto.createStartTime != null and pageDto.createStartTime != ''">
            and a.create_time >= #{pageDto.createStartTime}
        </if>
        <if test="pageDto.createEndTime != null and pageDto.createEndTime != ''">
            and a.create_time &lt;= #{pageDto.createEndTime}
        </if>
        <if test="pageDto.draftFlag != null">
            and a.draft_flag = #{pageDto.draftFlag}
        </if>
        order by a.create_time desc
    </select>
</mapper>
