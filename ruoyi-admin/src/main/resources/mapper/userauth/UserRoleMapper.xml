<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ruoyi.web.controller.jianguan.other.zlsk.strestservice.userauth.mapper.UserRoleMapper">

    <select id="select" parameterType="Map" resultType="Map">
        select * from sys_user_role where 1 = 1
        <if test="roleid != null and roleid != ''"> and ROLE_ID = #{roleid}</if>
        <if test="userid != null and userid != ''"> and USER_ID = #{userid}</if>
    </select>

    <select id="selectRoleByUser" parameterType="Map" resultType="Map">
        SELECT	t1.* FROM	sys_role t1	LEFT JOIN sys_user_role t2 ON t1.role_id = t2.role_id WHERE
        <if test="userid != null and userid != ''">t2.user_id  = #{userid}</if>
    </select>

    <insert id="insert" parameterType="Map">
        insert into ss_f_user_role
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userid != null">
                USERID,
            </if>
            <if test="roleid != null">
                ROLEID,
            </if>
            <if test="sttime != null">
                STTIME,
            </if>
            <if test="ststate != null">
                STSTATE,
            </if>
            <if test="storder != null">
                STORDER,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userid != null">
                #{userid,jdbcType=INTEGER},
            </if>
            <if test="roleid != null">
                #{roleid,jdbcType=INTEGER},
            </if>
            <if test="sttime != null">
                #{sttime,jdbcType=TIMESTAMP},
            </if>
            <if test="ststate != null">
                #{ststate,jdbcType=INTEGER},
            </if>
            <if test="storder != null">
                #{storder,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>

    <delete id="delete" parameterType="Integer">
        delete from ss_f_user_role where USERID = #{userId}
    </delete>
</mapper>
