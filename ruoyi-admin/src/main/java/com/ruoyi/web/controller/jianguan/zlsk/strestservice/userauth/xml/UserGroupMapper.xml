<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ruoyi.web.controller.jianguan.zlsk.strestservice.mapper.UserGroupMapper">

    <select id="select" parameterType="Map" resultType="Map">
        select * from ss_f_user_group where STSTATE = 1
        <if test="userid != null and userid != ''">and USERID = #{userid}</if>
        <if test="groupid != null and groupid != ''">and GROUPID = #{groupid}</if>
    </select>

    <insert id="insert" parameterType="Map">
        insert into ss_f_user_group
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userid != null">
                USERID,
            </if>
            <if test="groupid != null">
                GROUPID,
            </if>
            <if test="sttime != null">
                STTIME,
            </if>
            <if test="ststate != null">
                STSTATE,
            </if>
            <if test="storder != null">
                STORDER,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userid != null">
                #{userid,jdbcType=VARCHAR},
            </if>
            <if test="groupid != null">
                #{groupid,jdbcType=VARCHAR},
            </if>
            <if test="sttime != null">
                #{sttime,jdbcType=TIMESTAMP},
            </if>
            <if test="ststate != null">
                #{ststate,jdbcType=INTEGER},
            </if>
            <if test="storder != null">
                #{storder,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>

    <update id="update" parameterType="Map">
        update ss_f_user_group
        <set>
            <if test="userid != null">USERID = #{userid},</if>
            <if test="groupid != null">GROUPID = #{groupid},</if>
            <if test="sttime != null">STTIME = #{sttime},</if>
            <if test="ststate != null">STSTATE = #{ststate},</if>
            <if test="storder != null">STORDER = #{storder},</if>
        </set>
        where userid = #{userid}
    </update>

    <delete id="delete" parameterType="Integer">
        delete from ss_f_user_group where USERID = #{userid}
    </delete>
</mapper>
